<nav>
    <ul>
        <% for (let i = 0; i < navLinks.length; i++) { %>
            <li>
                <a href="<%= navLinks[i].href %>">
                    <span> <%= navLinks[i].text %> </span>
                </a>
            </li>
        <% } %>
    </ul>
</nav>